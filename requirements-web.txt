# Lean production dependencies for the Railway Content Studio service.
# Keep desktop/printing dependencies in pyproject.toml and out of this image.
fastapi>=0.115,<1
pydantic>=2,<3
PyYAML>=6,<7
uvicorn>=0.34,<1
# Product Hub Read API (PR07): xw_office.repositories.product_hub /
# xw_office.core.database pull these in transitively (SQLAlchemy for the ORM/session,
# psycopg2 as the desktop-compatible Postgres driver, psycopg as the driver for
# Railway's postgresql+psycopg URL, and python-dotenv because core.config imports it even
# though load_config() itself is never called from this lean web app).
sqlalchemy>=2,<3
psycopg2-binary>=2.9,<3
psycopg[binary]>=3.1,<4
python-dotenv>=1,<2
# Product Hub Wix push (PR11): xw_office.services.wix.product_details_client needs
# httpx. Importing anything under xw_office.services.wix.* also runs that package's
# __init__.py, which imports the legacy client.py too - its own transitive imports
# (core.performance_metrics, services.shipping.countries, wix.order_cache) are all
# stdlib-only, so httpx is the one and only addition this pulls in.
httpx>=0.27,<1
# Product Hub conflict advice: rank Wix mapping candidates by SKU/name similarity.
rapidfuzz>=3,<4
# Product Hub dealer sharing (PR12): XLSX export (web/routers/share_public.py).
openpyxl>=3.1,<4
